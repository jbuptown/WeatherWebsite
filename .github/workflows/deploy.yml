name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Run deploy on server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 139.100.235.137
          username: icss_predict
          key: ${{ secrets.ICCS }}
          script: |
            echo "SSH connected. Server-side deploy.sh is forced by sshd."